Jag har lagt till 'lang="en"' i html taggen, formaterat om divs till header, footer och sections utav kraven med inkluderade 'loading="Lazy"', 'labels', 'id', 'types' och ändrat plats på scriptet från header till slutet av body.

Det finns eventlisteners för Axe och PerformanceObserver-testningar för att jämföra eller säkerhetskolla resultat.

Det finns också bättre seperation på vissa css taggar precis som efterfrågat med 'proper' struktur.
